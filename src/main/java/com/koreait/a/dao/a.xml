<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.koreait.a.dao.StoreDAO">
	
	<!-- 
	<select id="selectStoreList" resultType="com.koreait.a.dto.Store">
		SELECT NO, OWERNAME, STORENAME, STORETEL, STORECATEGORI, STORETIME, STOREADDR,
			   STORECONTENT, STOREMENU, STORESNS, STOREHIT, FILENAME1, STORETABLE
		  FROM STORE
		 ORDER BY NO DESC;
	</select>
	 -->
	
	
	
</mapper>